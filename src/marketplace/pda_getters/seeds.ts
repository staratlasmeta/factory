export const MARKET_VARS_SEED = Buffer.from('market-vars');
export const ORDER_VAULT_SEED = Buffer.from('order-vault-account');
export const ORDER_VAULT_AUTH_SEED = Buffer.from('order-vault-auth');
export const REGISTERED_CURRENCY_SEED = Buffer.from('registered-currency');
export const OPEN_ORDERS_COUNTER = Buffer.from('open-orders-counter');
export const FEE_EXEMPT_SEED = Buffer.from('fee_exempt');
